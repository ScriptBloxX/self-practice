8/8/2568
Drawing_App

ไม่เคยลองเขียน Canvas มาก่อน เลยถือโอกาสลองดูครับ
ได้รู้ว่า canvas มี size 2 แบบคือ CSS กับ Attribute จำเป็นต้องใช้กำหนด size ผ่าน Attribute ถ้าจะให้ Offset ของ mouse ตรงกับ canvas จริงๆ
beginPath() ใช้สร้าง path ใหม่
moveTo() ใช้ย้ายตำแหน่งปัจจุบันไป x,y โดยไม่วาด,lineTo() ใช้ลากเส้นจากจุดปัจจุบันไปยัง x,y = ใช้สร้างเส้นจากจุด A -> B

9/8/2568
Drawing_App

วันนี้ได้ลองเพิ่ม features ใหม่ พอเพิ่ม size lineWidth แล้วพบว่าเส้นต่อกันแบบประหลาดๆ เป็นเหลี่ยมๆ จึงได้หาข้อมูลดู แล้วพบว่าต้องใช้ lineCap เพื่อกำหนดหัวเส้น และ lineJoin เพื่อปรับรอยต่อของเส้น

10/8/2568
วันนี้ได้ลองปรับให้เปลี่ยน cursor เปลี่ยนตามเครื่องมือที่เลือก ถ้า IsEraser==True cursor จะเปลี่ยนเป็น cell ถ้าไม่จะเป็น crosshair
และได้รู้เรื่อง globalCompositeOperation ที่ใช้ควบคุมวิธีวาดบน canvas โดยใช้ 'destination-out' สำหรับลบเส้นซึ่งเป็นวิธีที่ดีกว่าการระบายสีขาวทับไป เหมือนของวันที่แล้ว
